# ATMAN Extensions (peripheral only)

These modules implement the crew-voted capability gaps **around** the sealed core.

## Hard lock
Do **not** edit: private TSC, seal, `atman_core.py` immutability, identity `gate_policy.json`, `seal.py` / `crate.py` / `wake.py`.

## Modules
- `cliff_rope` — handoff resume package
- `offline_queue` — local jobs when cloud/usage dies
- `voice_bargein` — always-on barge-in scaffold
- `stop_gate` — Counsel Soft GO / STOP + privilege wall
- `receipt_ledger` — money claims with source + timestamp
- `battlemind` — crew routing bus
- `keepalive` — scoreboard + 7d/$0 kill watcher
- `cash_rail` — standing-greenlight spend door (chores blocked)
