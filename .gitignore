# The self is yours. None of this leaves your machine.
# Your own core and permanent memory. Git does not support inline comments.
tsc.json
psc.json
judge_trace.jsonl

# Sealed references — cut by YOUR hand with seal.py, never shipped
tsc.reference.json
core.reference.py

# Runtime state
quarantine/
__pycache__/
*.pyc
